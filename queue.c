#include "monty.h"

/**
 * queue - sets the format of data to queue.
 * @stack: doubly linked list stack.
 * @line_number: file line_number.
 */
void queue(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;

	info.queue = 1;
}
